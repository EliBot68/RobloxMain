-- Client Main Entry Point
print("🎮 Client starting...")

-- Get ReplicatedStorage reference
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Import SafeRequire utility
local SafeRequire = require(ReplicatedStorage.Shared.utils.SafeRequire)

-- Import controllers using SafeRequire
local GuiController = SafeRequire.require(script.controllers.GuiController)
local InputController = SafeRequire.require(script.controllers.InputController)
local PlayerController = SafeRequire.require(script.controllers.PlayerController)
local JumpController = SafeRequire.require(script.controllers.JumpController)
local GameController = SafeRequire.require(script.controllers.core.GameController)
local EnhancedUIController = SafeRequire.require(script.controllers.ui.EnhancedUIController)
local EnhancedGameplayController = SafeRequire.require(script.controllers.core.EnhancedGameplayController)
local RebirthController = SafeRequire.require(script.controllers.core.RebirthController)
local ShopController = SafeRequire.require(script.controllers.ShopController)
local FountainController = SafeRequire.require(script.controllers.FountainController)
local QuestController = SafeRequire.require(script.controllers.QuestController)
local TutorialController = SafeRequire.require(script.controllers.TutorialController)
local TeleportController = SafeRequire.require(script.controllers.TeleportController)
local ArchwayController = SafeRequire.require(script.controllers.ArchwayController)
local MysteryController = SafeRequire.require(script.controllers.MysteryController)
local XpController = SafeRequire.require(script.controllers.XpController)
local EventController = SafeRequire.require(script.controllers.EventController)
local StreakController = SafeRequire.require(script.controllers.StreakController)
local SettingsController = SafeRequire.require(script.controllers.SettingsController)
local GhostController = SafeRequire.require(script.controllers.GhostController)
local UpgradeController = SafeRequire.require(script.controllers.UpgradeController)
local ShareController = SafeRequire.require(script.controllers.ShareController)
local PolishController = SafeRequire.require(script.controllers.PolishController)
local WorldEventController = SafeRequire.require(script.controllers.WorldEventController)
local PuzzleController = SafeRequire.require(script.controllers.PuzzleController)
local TrophyController = SafeRequire.require(script.controllers.TrophyController)
local PetController = SafeRequire.require(script.controllers.PetController)
local ExclusiveController = SafeRequire.require(script.controllers.ExclusiveController)
local PartyController = SafeRequire.require(script.controllers.PartyController)
local MultiplayerController = SafeRequire.require(script.controllers.MultiplayerController)
local MiniGameController = SafeRequire.require(script.controllers.MiniGameController)
local BuffController = SafeRequire.require(script.controllers.BuffController)
local SpectatorController = SafeRequire.require(script.controllers.SpectatorController)
local TournamentController = SafeRequire.require(script.controllers.TournamentController)
local MobileUIController = SafeRequire.require(script.controllers.MobileUIController)
local EnhancedMobileUIController = SafeRequire.require(script.controllers.EnhancedMobileUIController)
local MobileSettingsController = SafeRequire.require(script.controllers.MobileSettingsController)
local WeeklyLeaderboardController = SafeRequire.require(script.controllers.WeeklyLeaderboardController)
local HubLeaderboardController = SafeRequire.require(script.controllers.HubLeaderboardController)
local GoalController = SafeRequire.require(script.controllers.GoalController)
local ContentController = SafeRequire.require(script.controllers.ContentController)
local ContentAdminPanel = SafeRequire.require(script.controllers.ContentAdminPanel)
local RebirthController = SafeRequire.require(script.controllers.core.RebirthController)
local HubController = SafeRequire.require(script.controllers.HubController)
local GameAudioController = SafeRequire.require(script.controllers.GameAudioController)
local CoinDisplayController = SafeRequire.require(script.controllers.ui.CoinDisplayController)
local CoinVisualController = SafeRequire.require(script.controllers.ui.CoinVisualController)
local AchievementController = SafeRequire.require(script.controllers.features.AchievementController)
local PurchaseConfirmationController = SafeRequire.require(script.controllers.PurchaseConfirmationController)
local DailyRewardController = SafeRequire.require(script.controllers.features.DailyRewardController)
local HubPuzzleController = SafeRequire.require(script.controllers.HubPuzzleController)
local XpDisplayController = SafeRequire.require(script.controllers.XpDisplayController)
local EnhancedUpgradeController = SafeRequire.require(script.controllers.EnhancedUpgradeController)
local TrailController = SafeRequire.require(script.controllers.TrailController)
local CrateMachineController = SafeRequire.require(script.controllers.CrateMachineController)
local TrailManagerController = SafeRequire.require(script.controllers.TrailManagerController)
local SaveFeedbackController = SafeRequire.require(script.controllers.SaveFeedbackController)
local MonetizationShopController = SafeRequire.require(script.controllers.MonetizationShopController)
local VIPStatusController = SafeRequire.require(script.controllers.VIPStatusController)
local MonetizationTestController = SafeRequire.require(script.controllers.MonetizationTestController)
local AntiCheatClient = SafeRequire.require(script.AntiCheatClient)

-- Initialize controllers
GuiController:Init()
InputController:Init()
PlayerController:Init()
JumpController:Init()
GameController:Init()
EnhancedUIController:Init() -- NEW: Enhanced UI with custom buttons and animations
EnhancedGameplayController:Init() -- NEW: Enhanced multi-difficulty gameplay mechanics
RebirthController:Init()
ShopController:Init()
MysteryController:Init()
XpController:Init()
EventController:Init()
StreakController:Init()
SettingsController:Init()
GhostController:Init()
UpgradeController:Init()
ShareController:Init()
PolishController:Init()
GoalController:Init()
ContentController:Init()
ContentAdminPanel:Init()
RebirthController:Init()
HubController:Init()
GameAudioController:Init()
CoinDisplayController:Init()
CoinVisualController:Init()
AchievementController:Init()
PurchaseConfirmationController:Init()
DailyRewardController:Init()
HubPuzzleController:Init()
XpDisplayController:Init()
EnhancedUpgradeController:Init()
TrailController:Init()
CrateMachineController:Init()
TrailManagerController:Init()
WorldEventController:Init()
PuzzleController:Init()
TrophyController:Init()
PetController:Init()
ExclusiveController:Init()
PartyController:Init()
MiniGameController:Init()
BuffController:Init()
SpectatorController:Init()
TournamentController:Init()

-- Initialize Anti-Cheat Client
if AntiCheatClient then
    AntiCheatClient:Init()
    print("🛡️ Anti-cheat client initialized")
end

-- FountainController, QuestController, TutorialController, TeleportController, and ArchwayController initialize themselves automatically

print("✅ Color Rush client initialized successfully!")